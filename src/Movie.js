class Movie {
  constructor(id, name, imageUrl, type, likes) {
    this.id = id
    this.name = name
    this.imageUrl = imageUrl
    this.type = type
    this.likes = likes
  }
}

export default Movie
