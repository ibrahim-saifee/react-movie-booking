class Movie {
  constructor(id, name, imageUrl, type, language, rating, likes) {
    this.id = id
    this.name = name
    this.imageUrl = imageUrl
    this.type = type
    this.language = language
    this.rating = rating
    this.likes = likes
  }
}

export default Movie
